diff --git a/OperationalPlanMS/Controllers/ProjectsController.cs b/OperationalPlanMS/Controllers/ProjectsController.cs
index deef542..a0a2f9b 100644
--- a/OperationalPlanMS/Controllers/ProjectsController.cs
+++ b/OperationalPlanMS/Controllers/ProjectsController.cs
@@ -221,5 +221,26 @@ namespace OperationalPlanMS.Controllers
             await _projectService.RecalculateProgressAsync(id);
             return RedirectToAction(nameof(Details), new { id });
         }
+
+        // POST: /Projects/ChangeStatus
+        [HttpPost, ValidateAntiForgeryToken]
+        public async Task<IActionResult> ChangeStatus(int projectId, Status newStatus, string reason,
+            ObstacleType? obstacleType, string? obstacleDescription, string? actionTaken, DateTime? expectedResumeDate)
+        {
+            var project = await _projectService.GetByIdAsync(projectId);
+            if (project == null) return NotFound();
+
+            var userRole = GetCurrentUserRole();
+            var userId = GetCurrentUserId();
+            if (!_projectService.CanAccess(project, userRole, userId)) return Forbid();
+
+            var (success, error) = await _projectService.ChangeStatusAsync(
+                projectId, newStatus, reason, obstacleType, obstacleDescription,
+                actionTaken, expectedResumeDate, userId);
+
+            TempData[success ? "SuccessMessage" : "ErrorMessage"] =
+                success ? "تم تغيير حالة المشروع بنجاح" : error;
+            return RedirectToAction(nameof(Details), new { id = projectId });
+        }
     }
 }
diff --git a/OperationalPlanMS/Data/AppDbContext.cs b/OperationalPlanMS/Data/AppDbContext.cs
index 8f5af42..c32f213 100644
--- a/OperationalPlanMS/Data/AppDbContext.cs
+++ b/OperationalPlanMS/Data/AppDbContext.cs
@@ -40,6 +40,7 @@ namespace OperationalPlanMS.Data
         public DbSet<ProjectSubObjective> ProjectSubObjectives { get; set; }
         public DbSet<SupportingUnitRepresentative> SupportingUnitRepresentatives { get; set; }
         public DbSet<StepTeamMember> StepTeamMembers { get; set; }
+        public DbSet<ProjectStatusChange> ProjectStatusChanges { get; set; }
 
 
 
@@ -472,6 +473,20 @@ namespace OperationalPlanMS.Data
 
                 entity.HasIndex(e => new { e.StepId, e.EmpNumber }).IsUnique();
             });
+
+            // ProjectStatusChange (سجل تغيير الحالة)
+            modelBuilder.Entity<ProjectStatusChange>(entity =>
+            {
+                entity.HasOne(e => e.Project)
+                    .WithMany(p => p.StatusChanges)
+                    .HasForeignKey(e => e.ProjectId)
+                    .OnDelete(DeleteBehavior.Cascade);
+
+                entity.HasOne(e => e.ChangedBy)
+                    .WithMany()
+                    .HasForeignKey(e => e.ChangedById)
+                    .OnDelete(DeleteBehavior.NoAction);
+            });
         }
     }
 }
diff --git a/OperationalPlanMS/Models/Entities/Project.cs b/OperationalPlanMS/Models/Entities/Project.cs
index 2416cf4..83972c1 100644
--- a/OperationalPlanMS/Models/Entities/Project.cs
+++ b/OperationalPlanMS/Models/Entities/Project.cs
@@ -136,6 +136,7 @@ namespace OperationalPlanMS.Models.Entities
         public virtual ICollection<ProjectSupportingUnit> SupportingUnits { get; set; } = new List<ProjectSupportingUnit>();
         public virtual ICollection<ProjectYearTarget> YearTargets { get; set; } = new List<ProjectYearTarget>();
         public virtual ICollection<ProjectSubObjective> ProjectSubObjectives { get; set; } = new List<ProjectSubObjective>();
+        public virtual ICollection<ProjectStatusChange> StatusChanges { get; set; } = new List<ProjectStatusChange>();
 
         // Computed Properties
         [NotMapped]
diff --git a/OperationalPlanMS/Models/Entities/ProjectStatusChange.cs b/OperationalPlanMS/Models/Entities/ProjectStatusChange.cs
new file mode 100644
index 0000000..7b86531
--- /dev/null
+++ b/OperationalPlanMS/Models/Entities/ProjectStatusChange.cs
@@ -0,0 +1,114 @@
+using System.ComponentModel.DataAnnotations;
+using System.ComponentModel.DataAnnotations.Schema;
+
+namespace OperationalPlanMS.Models.Entities
+{
+    /// <summary>
+    /// سجل تغيير حالة المشروع — يُسجَّل عند كل تغيير حالة
+    /// يتضمن السبب ونوع العائق والإجراء المتخذ
+    /// </summary>
+    [Table("ProjectStatusChanges")]
+    public class ProjectStatusChange
+    {
+        [Key]
+        public int Id { get; set; }
+
+        [Required]
+        public int ProjectId { get; set; }
+
+        [Required]
+        public Status OldStatus { get; set; }
+
+        [Required]
+        public Status NewStatus { get; set; }
+
+        /// <summary>
+        /// نوع العائق (إلزامي عند التوقف أو التأخر)
+        /// </summary>
+        public ObstacleType? ObstacleType { get; set; }
+
+        /// <summary>
+        /// وصف نوع العائق (عند اختيار "أخرى")
+        /// </summary>
+        [StringLength(200)]
+        public string? ObstacleDescription { get; set; }
+
+        /// <summary>
+        /// السبب / التبرير (إلزامي عند OnHold, Delayed, Cancelled)
+        /// </summary>
+        [Required]
+        [StringLength(1000)]
+        public string Reason { get; set; } = string.Empty;
+
+        /// <summary>
+        /// الإجراء المتخذ أو المخطط له
+        /// </summary>
+        [StringLength(1000)]
+        public string? ActionTaken { get; set; }
+
+        /// <summary>
+        /// التاريخ المتوقع للاستئناف (عند OnHold)
+        /// </summary>
+        [Column(TypeName = "date")]
+        public DateTime? ExpectedResumeDate { get; set; }
+
+        [Required]
+        public int ChangedById { get; set; }
+
+        public DateTime ChangedAt { get; set; } = DateTime.Now;
+
+        // ========== Navigation Properties ==========
+
+        [ForeignKey("ProjectId")]
+        public virtual Project Project { get; set; } = null!;
+
+        [ForeignKey("ChangedById")]
+        public virtual User ChangedBy { get; set; } = null!;
+
+        // ========== Computed Properties ==========
+
+        [NotMapped]
+        public bool IsNegativeChange =>
+            NewStatus == Status.OnHold ||
+            NewStatus == Status.Delayed ||
+            NewStatus == Status.Cancelled;
+
+        [NotMapped]
+        public string ObstacleTypeDisplayAr => ObstacleType switch
+        {
+            Models.ObstacleType.Financial => "مالي",
+            Models.ObstacleType.HumanResource => "نقص كوادر بشرية",
+            Models.ObstacleType.Technical => "تقني",
+            Models.ObstacleType.Administrative => "إداري",
+            Models.ObstacleType.External => "خارجي",
+            Models.ObstacleType.Procurement => "مشتريات / توريد",
+            Models.ObstacleType.Other => !string.IsNullOrEmpty(ObstacleDescription) ? ObstacleDescription : "أخرى",
+            _ => ""
+        };
+
+        [NotMapped]
+        public string NewStatusDisplayAr => NewStatus switch
+        {
+            Status.Draft => "مسودة",
+            Status.Pending => "معلّق",
+            Status.Approved => "معتمد",
+            Status.InProgress => "جاري التنفيذ",
+            Status.OnHold => "متوقف مؤقتاً",
+            Status.Completed => "مكتمل",
+            Status.Cancelled => "ملغي",
+            Status.Delayed => "متأخر",
+            _ => ""
+        };
+
+        [NotMapped]
+        public string StatusBadgeClass => NewStatus switch
+        {
+            Status.InProgress => "bg-primary",
+            Status.Completed => "bg-success",
+            Status.OnHold => "bg-warning text-dark",
+            Status.Delayed => "bg-danger",
+            Status.Cancelled => "bg-secondary",
+            _ => "bg-info"
+        };
+    }
+}
diff --git a/OperationalPlanMS/Models/Enums.cs b/OperationalPlanMS/Models/Enums.cs
index 40cdcb4..98a3176 100644
--- a/OperationalPlanMS/Models/Enums.cs
+++ b/OperationalPlanMS/Models/Enums.cs
@@ -94,4 +94,18 @@ namespace OperationalPlanMS.Models
         Contributor = 1,    // تعديل + إضافة
         FullAccess = 2      // كل شيء بما فيه الحذف
     }
+
+    /// <summary>
+    /// نوع العائق / سبب التأخر أو التوقف
+    /// </summary>
+    public enum ObstacleType
+    {
+        Financial = 0,        // مالي
+        HumanResource = 1,    // نقص كوادر بشرية
+        Technical = 2,        // تقني
+        Administrative = 3,   // إداري
+        External = 4,         // خارجي (جهة خارجية)
+        Procurement = 5,      // مشتريات / توريد
+        Other = 6             // أخرى
+    }
 }
\ No newline at end of file
diff --git a/OperationalPlanMS/Models/ViewModels/ProjectViewModels.cs b/OperationalPlanMS/Models/ViewModels/ProjectViewModels.cs
index 3d0f9af..9ac8df7 100644
--- a/OperationalPlanMS/Models/ViewModels/ProjectViewModels.cs
+++ b/OperationalPlanMS/Models/ViewModels/ProjectViewModels.cs
@@ -522,6 +522,7 @@ namespace OperationalPlanMS.Models.ViewModels
         public Project Project { get; set; } = null!;
         public List<Step> Steps { get; set; } = new();
         public List<ProgressUpdate> Notes { get; set; } = new();
+        public List<ProjectStatusChange> StatusChanges { get; set; } = new();
         
         public List<ProjectRequirement> Requirements { get; set; } = new();
         public List<ProjectKPI> KPIs { get; set; } = new();
diff --git a/OperationalPlanMS/Services/ProjectService.cs b/OperationalPlanMS/Services/ProjectService.cs
index 01297d6..3d11b55 100644
--- a/OperationalPlanMS/Services/ProjectService.cs
+++ b/OperationalPlanMS/Services/ProjectService.cs
@@ -27,6 +27,9 @@ namespace OperationalPlanMS.Services
         Task<(bool Success, string? Error)> EditNoteAsync(int noteId, int projectId, string notes);
         Task<(bool Success, string? Error)> DeleteNoteAsync(int noteId, int projectId);
 
+        // تغيير حالة المشروع مع السبب
+        Task<(bool Success, string? Error)> ChangeStatusAsync(int projectId, Status newStatus, string reason, ObstacleType? obstacleType, string? obstacleDescription, string? actionTaken, DateTime? expectedResumeDate, int changedById);
+
         // إعادة حساب التقدم
         Task<decimal> RecalculateProgressAsync(int projectId);
         Task UpdateProjectProgressAsync(int projectId);
@@ -180,6 +183,8 @@ namespace OperationalPlanMS.Services
                 Project = project, Steps = steps,
                 Notes = await _db.ProgressUpdates.Where(p => p.ProjectId == id)
                     .Include(p => p.CreatedBy).OrderByDescending(p => p.CreatedAt).Take(20).ToListAsync(),
+                StatusChanges = await _db.ProjectStatusChanges.Where(sc => sc.ProjectId == id)
+                    .Include(sc => sc.ChangedBy).OrderByDescending(sc => sc.ChangedAt).ToListAsync(),
                 Requirements = requirements, KPIs = kpis,
                 SupportingEntities = supportingEntities, YearTargets = yearTargetDisplayItems
             };
@@ -358,6 +363,42 @@ namespace OperationalPlanMS.Services
             return (true, null);
         }
 
+        public async Task<(bool Success, string? Error)> ChangeStatusAsync(
+            int projectId, Status newStatus, string reason,
+            ObstacleType? obstacleType, string? obstacleDescription,
+            string? actionTaken, DateTime? expectedResumeDate, int changedById)
+        {
+            var project = await _db.Projects.FindAsync(projectId);
+            if (project == null || project.IsDeleted) return (false, "المشروع غير موجود");
+
+            if (string.IsNullOrWhiteSpace(reason)) return (false, "السبب مطلوب");
+
+            var isNegative = newStatus == Status.OnHold || newStatus == Status.Delayed || newStatus == Status.Cancelled;
+            if (isNegative && !obstacleType.HasValue) return (false, "نوع العائق مطلوب");
+
+            var statusChange = new ProjectStatusChange
+            {
+                ProjectId = projectId,
+                OldStatus = project.Status,
+                NewStatus = newStatus,
+                ObstacleType = obstacleType,
+                ObstacleDescription = obstacleDescription,
+                Reason = reason,
+                ActionTaken = actionTaken,
+                ExpectedResumeDate = expectedResumeDate,
+                ChangedById = changedById,
+                ChangedAt = DateTime.Now
+            };
+            _db.ProjectStatusChanges.Add(statusChange);
+
+            project.Status = newStatus;
+            project.LastModifiedById = changedById;
+            project.LastModifiedAt = DateTime.Now;
+
+            await _db.SaveChangesAsync();
+            return (true, null);
+        }
+
         #endregion
 
         #region التقدم
diff --git a/OperationalPlanMS/Views/Projects/Details.cshtml b/OperationalPlanMS/Views/Projects/Details.cshtml
index e2a3bbe..9b589d3 100644
--- a/OperationalPlanMS/Views/Projects/Details.cshtml
+++ b/OperationalPlanMS/Views/Projects/Details.cshtml
@@ -48,6 +48,9 @@
     <div class="page-header-actions">
         @if (canEdit)
         {
+            <button type="button" class="btn btn-outline-info" data-bs-toggle="modal" data-bs-target="#changeStatusModal">
+                <i class="bi bi-arrow-repeat"></i>تغيير الحالة
+            </button>
             <a href="/Projects/Edit/@Model.Project.Id" class="btn btn-warning">
                 <i class="bi bi-pencil"></i>تعديل
             </a>
@@ -766,9 +769,150 @@
                 }
             </div>
         </div>
+
+        <!-- سجل تغيير الحالة -->
+        @if (Model.StatusChanges.Any())
+        {
+            <div class="card mt-3">
+                <div class="card-header d-flex justify-content-between align-items-center">
+                    <span><i class="bi bi-arrow-repeat me-2"></i>سجل تغيير الحالة</span>
+                    <span class="badge bg-secondary">@Model.StatusChanges.Count</span>
+                </div>
+                <div class="card-body p-0">
+                    <div class="list-group list-group-flush">
+                        @foreach (var sc in Model.StatusChanges)
+                        {
+                            <div class="list-group-item py-3">
+                                <div class="d-flex justify-content-between align-items-start mb-1">
+                                    <span class="badge @sc.StatusBadgeClass">@sc.NewStatusDisplayAr</span>
+                                    <small class="text-muted">@sc.ChangedAt.ToString("yyyy-MM-dd HH:mm")</small>
+                                </div>
+                                <p class="mb-1 small"><strong>السبب:</strong> @sc.Reason</p>
+                                @if (sc.ObstacleType.HasValue)
+                                {
+                                    <p class="mb-1 small"><strong>نوع العائق:</strong> <span class="badge bg-light text-dark">@sc.ObstacleTypeDisplayAr</span></p>
+                                }
+                                @if (!string.IsNullOrEmpty(sc.ActionTaken))
+                                {
+                                    <p class="mb-1 small"><strong>الإجراء:</strong> @sc.ActionTaken</p>
+                                }
+                                @if (sc.ExpectedResumeDate.HasValue)
+                                {
+                                    <p class="mb-1 small"><strong>الاستئناف المتوقع:</strong> @sc.ExpectedResumeDate.Value.ToString("yyyy-MM-dd")</p>
+                                }
+                                <small class="text-muted">بواسطة: @sc.ChangedBy?.FullNameAr</small>
+                            </div>
+                        }
+                    </div>
+                </div>
+            </div>
+        }
+    </div>
+</div>
+
+<!-- Modal: تغيير حالة المشروع -->
+<div class="modal fade" id="changeStatusModal" tabindex="-1">
+    <div class="modal-dialog modal-lg">
+        <form method="post" action="/Projects/ChangeStatus">
+            @Html.AntiForgeryToken()
+            <input type="hidden" name="projectId" value="@Model.Project.Id" />
+            <div class="modal-content">
+                <div class="modal-header">
+                    <h5 class="modal-title"><i class="bi bi-arrow-repeat me-2"></i>تغيير حالة المشروع</h5>
+                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
+                </div>
+                <div class="modal-body">
+                    <div class="mb-3">
+                        <label class="form-label fw-bold">الحالة الحالية</label>
+                        <div>
+                            @{
+                                var currentStatusText = Model.Project.Status switch
+                                {
+                                    Status.Draft => "مسودة",
+                                    Status.InProgress => "جاري التنفيذ",
+                                    Status.OnHold => "متوقف مؤقتاً",
+                                    Status.Completed => "مكتمل",
+                                    Status.Cancelled => "ملغي",
+                                    Status.Delayed => "متأخر",
+                                    _ => Model.Project.Status.ToString()
+                                };
+                            }
+                            <span class="badge bg-info">@currentStatusText</span>
+                        </div>
+                    </div>
+
+                    <div class="mb-3">
+                        <label for="newStatus" class="form-label fw-bold">الحالة الجديدة <span class="text-danger">*</span></label>
+                        <select name="newStatus" id="newStatus" class="form-select" required onchange="onStatusChange()">
+                            <option value="">-- اختر الحالة --</option>
+                            <option value="@((int)Status.InProgress)">جاري التنفيذ</option>
+                            <option value="@((int)Status.OnHold)">متوقف مؤقتاً</option>
+                            <option value="@((int)Status.Delayed)">متأخر</option>
+                            <option value="@((int)Status.Completed)">مكتمل</option>
+                            <option value="@((int)Status.Cancelled)">ملغي</option>
+                        </select>
+                    </div>
+
+                    <div class="mb-3" id="obstacleTypeGroup" style="display:none;">
+                        <label for="obstacleType" class="form-label fw-bold">نوع العائق <span class="text-danger">*</span></label>
+                        <select name="obstacleType" id="obstacleType" class="form-select" onchange="onObstacleTypeChange()">
+                            <option value="">-- اختر النوع --</option>
+                            <option value="0">مالي</option>
+                            <option value="1">نقص كوادر بشرية</option>
+                            <option value="2">تقني</option>
+                            <option value="3">إداري</option>
+                            <option value="4">خارجي (جهة خارجية)</option>
+                            <option value="5">مشتريات / توريد</option>
+                            <option value="6">أخرى</option>
+                        </select>
+                    </div>
+
+                    <div class="mb-3" id="obstacleDescGroup" style="display:none;">
+                        <label for="obstacleDescription" class="form-label fw-bold">وصف نوع العائق</label>
+                        <input type="text" name="obstacleDescription" id="obstacleDescription" class="form-control" maxlength="200" placeholder="حدد نوع العائق..." />
+                    </div>
+
+                    <div class="mb-3">
+                        <label for="reason" class="form-label fw-bold">السبب / التبرير <span class="text-danger">*</span></label>
+                        <textarea name="reason" id="reason" class="form-control" rows="3" required maxlength="1000" placeholder="اكتب سبب تغيير الحالة..."></textarea>
+                    </div>
+
+                    <div class="mb-3" id="actionTakenGroup" style="display:none;">
+                        <label for="actionTaken" class="form-label fw-bold">الإجراء المتخذ / المخطط</label>
+                        <textarea name="actionTaken" id="actionTaken" class="form-control" rows="2" maxlength="1000" placeholder="ما الإجراء المتخذ أو المخطط لحل المشكلة؟"></textarea>
+                    </div>
+
+                    <div class="mb-3" id="resumeDateGroup" style="display:none;">
+                        <label for="expectedResumeDate" class="form-label fw-bold">التاريخ المتوقع للاستئناف</label>
+                        <input type="date" name="expectedResumeDate" id="expectedResumeDate" class="form-control" />
+                    </div>
+                </div>
+                <div class="modal-footer">
+                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
+                    <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>حفظ التغيير</button>
+                </div>
+            </div>
+        </form>
     </div>
 </div>
 
 @section Scripts {
     <partial name="Partials/_NotesSectionScripts" />
+    <script>
+        function onStatusChange() {
+            var val = parseInt(document.getElementById('newStatus').value);
+            var isNegative = (val === @((int)Status.OnHold) || val === @((int)Status.Delayed) || val === @((int)Status.Cancelled));
+            document.getElementById('obstacleTypeGroup').style.display = isNegative ? '' : 'none';
+            document.getElementById('actionTakenGroup').style.display = isNegative ? '' : 'none';
+            document.getElementById('resumeDateGroup').style.display = (val === @((int)Status.OnHold)) ? '' : 'none';
+            if (!isNegative) {
+                document.getElementById('obstacleType').value = '';
+                document.getElementById('obstacleDescGroup').style.display = 'none';
+            }
+        }
+        function onObstacleTypeChange() {
+            var val = document.getElementById('obstacleType').value;
+            document.getElementById('obstacleDescGroup').style.display = (val === '6') ? '' : 'none';
+        }
+    </script>
 }
